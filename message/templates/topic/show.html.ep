% layout 'default';
% title 'Téma: '.$topic->name;

<div class="d-flex">
    %= render_parents $topic
    <div class="breadcrumb rounded-right" style="border-radius: 0px">
        %= include 'topic/_toolbar'
    </div>
</div>
%= render_topic_tree $topic

% while (my $message = $messages->next) {
    %= include 'message/_show', message => $message
% }
